module.exports = {
  semi: true,
  singleQuote: true,
  trailingComma: 'es5', // Use 'all' for ES5+ compatible trailing commas (functions, etc.)
  printWidth: 100, // Changed from 100 to 100 as per original request snippet
  tabWidth: 2,
  bracketSpacing: true, // Add space inside brackets, e.g. { foo: bar }
  arrowParens: 'always', // Always include parens around arrow function parameters, e.g. (x) => x
};
